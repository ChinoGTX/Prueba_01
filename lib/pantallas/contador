import 'package:flutter/material.dart';

class contador extends StatessWidget {
    const contador({Key key});

    @override
    Widget build(BuildContext context) {
        return Scaffold(
            body: Center(
                child: Text('Cantidad de clicks'),
            ),
        );
    }
}